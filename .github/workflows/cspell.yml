name: Check Spelling
on:
  pull_request: null
  push: null
jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.1
      - uses: streetsidesoftware/cspell-action@v5.3.0
